* 与传统的 asp.net 的写法，进行性能对比  ✅
* trace_me 的能力的接入
* metrics 上报，如何把多个 ulong 自动变成 metrics 文本?
* 生成 client 类型的代码
* metrics push 能力，方便做压测时候的观测
* 在 k8s 环境，部署 20 核的容器进行压测
* 还不支持 GET 请求访问
* metrics push 的能力考虑支持，后续需要方便的做压测
* histogram 的支持


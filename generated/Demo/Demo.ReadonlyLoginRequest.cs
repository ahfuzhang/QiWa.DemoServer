// Code generated by https://github.com/ahfuzhang/BaoHuLu. DO NOT EDIT.
// source: Generated.Demo

#pragma warning disable CS8600, CS8601, CS8602, CS8618, CS8625

using System;
using System.Buffers.Text;
using System.Collections.Generic;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using QiWa.Common;
using static QiWa.Common.ProtobufUtils;

namespace Generated.Demo;


// ============================================================================
// ReadonlyLoginRequest — immutable view over decoded protobuf bytes
// ============================================================================

public partial struct ReadonlyLoginRequest : IResettable, IDecoder
{
    [JsonPropertyName("user_name")]
    public string UserName;
    [JsonPropertyName("user_password_sha256")]
    public string UserPasswordSha256;

    // ── FromProtobuf ─────────────────────────────────────────────────────────

    public Error FromProtobuf(ReadOnlySpan<byte> binary)
    {
        // local decode variables

        int _pos = 0;
        while (_pos < binary.Length)
        {
            if (!TryReadVarint(binary, _pos, out ulong _tagRaw, out int _tagBytes))
                return Error.WithLoc(1, "failed to read tag");
            _pos += _tagBytes;
            int _fieldNum = (int)(_tagRaw >> 3);
            int _wireType = (int)(_tagRaw & 7);
            switch (_fieldNum)
            {
                case LoginRequestTags.TagUserName: // UserName
                {
                    if (!TryReadVarint(binary, _pos, out ulong _slenUserName, out int _slbUserName))
                        return Error.WithLoc(1, "bad string UserName");
                    _pos += _slbUserName;
                    this.UserName = Encoding.UTF8.GetString(binary.Slice(_pos, (int)_slenUserName));
                    _pos += (int)_slenUserName;
                }
                    break;
                case LoginRequestTags.TagUserPasswordSha256: // UserPasswordSha256
                {
                    if (!TryReadVarint(binary, _pos, out ulong _slenUserPasswordSha256, out int _slbUserPasswordSha256))
                        return Error.WithLoc(1, "bad string UserPasswordSha256");
                    _pos += _slbUserPasswordSha256;
                    this.UserPasswordSha256 = Encoding.UTF8.GetString(binary.Slice(_pos, (int)_slenUserPasswordSha256));
                    _pos += (int)_slenUserPasswordSha256;
                }
                    break;
                default:
                    if (!SkipField(binary, _pos, _wireType, out int _skip))
                        return Error.WithLoc(1, "truncated binary data");
                    _pos += _skip;
                    break;
            }
        }
        return default;
    }

    // ── FromJSON ─────────────────────────────────────────────────────────────

    public Error FromJSON(ReadOnlySpan<byte> text)
    {
        try
        {
            var _reader = new Utf8JsonReader(text, new JsonReaderOptions
            {
                AllowTrailingCommas = true,
                CommentHandling     = JsonCommentHandling.Skip
            });
            return FromJSONReader(ref _reader);
        }
        catch (System.Text.Json.JsonException ex)
        {
            return Error.WithLoc(1, ex.Message);
        }
    }

    internal Error FromJSONReader(ref Utf8JsonReader reader)
    {
        // If the reader is freshly created (None) or not yet at StartObject, advance.
        // When called from a nested context, caller already advanced to StartObject.
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            if (!reader.Read() || reader.TokenType != JsonTokenType.StartObject)
                return Error.WithLoc(1, "expected {");
        }

        // local decode variables

        while (reader.Read() && reader.TokenType != JsonTokenType.EndObject)
        {
            if (reader.TokenType != JsonTokenType.PropertyName)
                return Error.WithLoc(1, "expected property name");
            var _propSpan = reader.ValueSpan;
            if (!reader.Read()) return Error.WithLoc(1, "unexpected end");
            if (_propSpan.SequenceEqual(LoginRequestTags.JsonKeyUserName))
                    this.UserName = reader.GetString() ?? string.Empty;
            else if (_propSpan.SequenceEqual(LoginRequestTags.JsonKeyUserPasswordSha256))
                    this.UserPasswordSha256 = reader.GetString() ?? string.Empty;
            else { reader.Skip(); }
        }
        return default;
    }

    // ── Clone ─────────────────────────────────────────────────────────────────

    public void Clone(ref LoginRequest dst)
    {
        dst.UserName = UserName;
        dst.UserPasswordSha256 = UserPasswordSha256;
    }

    // ── Reset ─────────────────────────────────────────────────────────────────

    public void Reset()
    {
        UserName = string.Empty;
        UserPasswordSha256 = string.Empty;
    }
}


// Code generated by https://github.com/ahfuzhang/BaoHuLu. DO NOT EDIT.
// source: Generated.Demo

#pragma warning disable CS8600, CS8601, CS8602, CS8618, CS8625

using System;
using System.Buffers.Text;
using System.Collections.Generic;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using QiWa.Common;
using static QiWa.Common.ProtobufUtils;

namespace Generated.Demo;


// ============================================================================
// ReadonlySetUserTagsRequest — immutable view over decoded protobuf bytes
// ============================================================================

public partial struct ReadonlySetUserTagsRequest : IResettable, IDecoder
{
    [JsonPropertyName("tags")]
    public Dictionary<string, string> Tags;
    [JsonPropertyName("user_session")]
    public string UserSession;
    [JsonPropertyName("user_id")]
    public uint UserID;

    // ── FromProtobuf ─────────────────────────────────────────────────────────

    public Error FromProtobuf(ReadOnlySpan<byte> binary)
    {
        // local decode variables
        var _TagsDict = this.Tags ?? new Dictionary<string, string>();

        int _pos = 0;
        while (_pos < binary.Length)
        {
            if (!TryReadVarint(binary, _pos, out ulong _tagRaw, out int _tagBytes))
                return Error.WithLoc(1, "failed to read tag");
            _pos += _tagBytes;
            int _fieldNum = (int)(_tagRaw >> 3);
            int _wireType = (int)(_tagRaw & 7);
            switch (_fieldNum)
            {
                case SetUserTagsRequestTags.TagTags: // Tags
                {
                    if (!TryReadVarint(binary, _pos, out ulong _elenTags, out int _elbTags))
                        return Error.WithLoc(1, "bad map Tags");
                    _pos += _elbTags;
                    var _entBinTags = binary.Slice(_pos, (int)_elenTags);
                    string _entKeyTags = string.Empty;
                    string _entValTags = string.Empty;
                    int _epTags = 0;
                    while (_epTags < _entBinTags.Length)
                    {
                        if (!TryReadVarint(_entBinTags, _epTags, out ulong _etagTags, out int _etbTags)) break;
                        _epTags += _etbTags;
                        int _efTags = (int)(_etagTags >> 3);
                        int _ewTags = (int)(_etagTags & 7);
                        if (_efTags == 1) // map key
                        {
                            if (!TryReadVarint(_entBinTags, _epTags, out ulong _kslTags, out int _ksbTags)) break;
                            _epTags += _ksbTags;
                            _entKeyTags = Encoding.UTF8.GetString(_entBinTags.Slice(_epTags, (int)_kslTags));
                            _epTags += (int)_kslTags;
                        }
                        else if (_efTags == 2) // map value
                        {
                            if (!TryReadVarint(_entBinTags, _epTags, out ulong _vslTags, out int _vsbTags)) break;
                            _epTags += _vsbTags;
                            _entValTags = Encoding.UTF8.GetString(_entBinTags.Slice(_epTags, (int)_vslTags));
                            _epTags += (int)_vslTags;
                        }
                        else
                        {
                            if (!SkipField(_entBinTags, _epTags, _ewTags, out int _eskipTags)) break;
                            _epTags += _eskipTags;
                        }
                    }
                    _TagsDict[_entKeyTags] = _entValTags;
                    _pos += (int)_elenTags;
                }
                    break;
                case SetUserTagsRequestTags.TagUserSession: // UserSession
                {
                    if (!TryReadVarint(binary, _pos, out ulong _slenUserSession, out int _slbUserSession))
                        return Error.WithLoc(1, "bad string UserSession");
                    _pos += _slbUserSession;
                    this.UserSession = Encoding.UTF8.GetString(binary.Slice(_pos, (int)_slenUserSession));
                    _pos += (int)_slenUserSession;
                }
                    break;
                case SetUserTagsRequestTags.TagUserID: // UserID
                {
                    // varint / enum
                    if (!TryReadVarint(binary, _pos, out ulong _varvUserID, out int _varbUserID))
                        return Error.WithLoc(1, "bad varint UserID");
                    this.UserID = (uint)_varvUserID;
                    _pos += _varbUserID;
                }
                    break;
                default:
                    if (!SkipField(binary, _pos, _wireType, out int _skip))
                        return Error.WithLoc(1, "truncated binary data");
                    _pos += _skip;
                    break;
            }
        }
        this.Tags = _TagsDict;
        return default;
    }

    // ── FromJSON ─────────────────────────────────────────────────────────────

    public Error FromJSON(ReadOnlySpan<byte> text)
    {
        try
        {
            var _reader = new Utf8JsonReader(text, new JsonReaderOptions
            {
                AllowTrailingCommas = true,
                CommentHandling     = JsonCommentHandling.Skip
            });
            return FromJSONReader(ref _reader);
        }
        catch (System.Text.Json.JsonException ex)
        {
            return Error.WithLoc(1, ex.Message);
        }
    }

    internal Error FromJSONReader(ref Utf8JsonReader reader)
    {
        // If the reader is freshly created (None) or not yet at StartObject, advance.
        // When called from a nested context, caller already advanced to StartObject.
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            if (!reader.Read() || reader.TokenType != JsonTokenType.StartObject)
                return Error.WithLoc(1, "expected {");
        }

        // local decode variables
        var _TagsDict = this.Tags ?? new Dictionary<string, string>();

        while (reader.Read() && reader.TokenType != JsonTokenType.EndObject)
        {
            if (reader.TokenType != JsonTokenType.PropertyName)
                return Error.WithLoc(1, "expected property name");
            var _propSpan = reader.ValueSpan;
            if (!reader.Read()) return Error.WithLoc(1, "unexpected end");
            if (_propSpan.SequenceEqual(SetUserTagsRequestTags.JsonKeyTags))
                {
                    if (reader.TokenType == JsonTokenType.StartObject)
                    {
                        while (reader.Read() && reader.TokenType != JsonTokenType.EndObject)
                        {
                            string _mkeTags = reader.GetString()!;
                            reader.Read();
                            _TagsDict[_mkeTags] = reader.GetString() ?? string.Empty;
                        }
                    }
                }
            else if (_propSpan.SequenceEqual(SetUserTagsRequestTags.JsonKeyUserSession))
                    this.UserSession = reader.GetString() ?? string.Empty;
            else if (_propSpan.SequenceEqual(SetUserTagsRequestTags.JsonKeyUserID))
                    { if (!Utf8Parser.TryParse(reader.ValueSpan, out int _ivUserID, out _)) return Error.WithLoc(1, "bad UserID"); this.UserID = (uint)_ivUserID; }
            else { reader.Skip(); }
        }
        this.Tags = _TagsDict;
        return default;
    }

    // ── Clone ─────────────────────────────────────────────────────────────────

    public void Clone(ref SetUserTagsRequest dst)
    {
        if (dst.Tags == null)
            dst.Tags = new Dictionary<string, string>(Tags?.Count ?? 0);
        else
            dst.Tags.Clear();
        if (Tags != null)
            foreach (var _kvTags in Tags)
                dst.Tags[_kvTags.Key] = _kvTags.Value;
        dst.UserSession = UserSession;
        dst.UserID = UserID;
    }

    // ── Reset ─────────────────────────────────────────────────────────────────

    public void Reset()
    {
        Tags?.Clear();
        UserSession = string.Empty;
        UserID = 0;
    }
}

